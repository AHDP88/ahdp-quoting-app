# Master Pricing Deck Path Update

This zip contains only the latest Master Pricing deck path update files.

Replace the matching files in your GitHub Desktop repo folder with the files from this zip, preserving the folder paths:

- server/quoteCalculation.ts
- client/src/components/DimensionsForm.tsx
- client/src/lib/dropdownOptions.ts
- server/settingsRoutes.ts
- server/storage.ts

After replacing the files, commit the changes with this message:

Use master pricing itemCodes for deck materials

Then push to origin from GitHub Desktop.
